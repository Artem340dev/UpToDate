/*console.log($('.main-section-text-container').height())

$(window).scroll(function () {
	let scrollTop = $(window).scrollTop();

	if (scrollTop < 250) {
		var currentTop = $('.main-section-text-container').css('top');
		$('.main-section-text-container').css({'top': '-=10px'});
	}
});*/